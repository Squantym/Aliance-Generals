# Секреты — никогда не публиковать!
.env

# Локальные данные (JSON-файлы, создаются автоматически при запуске без MongoDB)
/data/

# Зависимости Node.js (устанавливаются через npm install)
node_modules/

# Логи и временные файлы
*.log
npm-debug.log*
.DS_Store
