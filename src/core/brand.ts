// ═══════════════════════════════════════════════════════════════════
// src/core/brand.ts — название игры в одном месте
//
// Раньше «Генералы» было вписано руками в письмах, в двухфакторной
// авторизации, в заголовках вкладок и в манифесте. Переименование
// означало обход десятка файлов, и один-два всегда оставались со старым
// названием — обычно те, что видит не владелец, а игрок: приложение на
// телефоне, приложение-аутентификатор, письмо в почте.
//
// Теперь название берётся отсюда. Клиентская часть читает его из
// /api/config, чтобы не держать вторую копию в public/js.
// ═══════════════════════════════════════════════════════════════════

// Полное название по-русски — то, что видит игрок.
const GAME_NAME = 'Альянс Генералов';

// Латиницей: имя отправителя писем и подпись в приложении-аутентификаторе.
// Написание намеренно совпадает с доменом aliance-general.ru.
const GAME_NAME_EN = 'Aliance Generals';

// Заглавными — для шапок и заголовков вкладок.
const GAME_NAME_CAPS = 'АЛЬЯНС ГЕНЕРАЛОВ';

export = { GAME_NAME, GAME_NAME_EN, GAME_NAME_CAPS };
